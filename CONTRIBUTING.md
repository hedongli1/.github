# Contributing · 参与贡献

感谢你愿意为本账号下的开源项目做贡献 🎉 本文件是该账号所有仓库的通用默认贡献指南。

## 通用流程

1. **先开 issue**：较大的改动、行为变更或新功能，请先开 issue 讨论，避免无效工作。
2. **Fork & 分支**：从 `main` 拉出描述性分支，如 `fix/xxx`、`feat/xxx`。
3. **小步提交**：一次提交只做一件事，提交信息用 `<type>(<scope>): <subject>`，
   例如 `fix(parser): 修正边界判空`、`docs(readme): 补充本地运行说明`。
4. **自测**：改动后请本地跑通项目自带的检查 / 自检脚本（如 `node scripts/selftest.js`、
   `python -m compileall`），确保 CI 为绿。
5. **发 PR**：填写 PR 模板中的检查清单，说明「改了什么 / 为什么 / 怎么验证」。

## 通用设计原则（本账号项目共同遵循）

- **零 LLM / 可复现**：数据类项目尽量不依赖大模型做判断，结果应确定、可审计。
- **零或轻依赖**：优先使用标准库 / 少量成熟依赖，降低供应链风险。
- **诚实优先**：数据来源、覆盖范围、已知缺口如实标注，不夸大、不伪装完整。
- **可追溯**：每个结论都应能追溯到具体数据源或提交。
- **自动化**：能被 CI / 定时任务验证的，就不要靠人工记忆。

## 提交规范

- 不提交构建产物、密钥、个人隐私数据。
- 大文件请谨慎，必要时说明理由。
- 涉及数据源许可的改动，请同步更新 `NOTICE.md` / `SECURITY.md` 相关说明。

## 行为准则

参与本项目即表示你同意遵守 [CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)。

## 安全问题

请勿通过公开 issue 报告安全漏洞，见 [SECURITY.md](./SECURITY.md)。
